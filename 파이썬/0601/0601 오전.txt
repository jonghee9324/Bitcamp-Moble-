c 자바차이 // 클래스 유무, 클래스는 객체가 아니다, 
java //  포인터가 없음, 클래스 있음, jvm이 있어 호환성이 뛰어나지만 c보다 느림
c // 빠름, 포인터로 직접 메모리에 접근 가능, 따라서 사이즈가 작은 프로그램일수록 효율이 올라감 
c c++차이 // 클래스 유무
객체 // 메모리에 할당된 것, 메모리에 적재된 클래스

객체지향 // 껍데기를 열심히 만들고 필요한 것만 메모리에  할당 
순차지향 // 메모리에 다 할당
